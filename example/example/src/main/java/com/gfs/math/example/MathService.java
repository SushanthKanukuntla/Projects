package com.gfs.math.example;

public class MathService {

	/**
	 * 
	 * Please implement this method to return the number in the array that is
	 * closest to zero.
	 * 
	 * If there are two equally close to zero elements like 2 and -2 - consider
	 * the positive element to be "closer" to zero.
	 * 
	 * If the input is null or empty, throw an IllegalArgumentException.
	 * 
	 * @param intValues
	 *            an <code>int[]</code>
	 * @return the integer from the passed in array that is closest to zero
	 */
	public int getClosestToZero(int[] intValues) {
		throw new RuntimeException();
	}
}
